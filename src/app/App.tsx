import React, { FC } from 'react';

import '../App.css';

import { AppRoutes } from './AppRoutes/AppRoutes';

const App: FC = () => {
  return (
    <div>
      <AppRoutes />
    </div>
  );
};

export default App;
